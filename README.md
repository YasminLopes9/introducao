 Yasmin Lopes DS3- O let é usado para criar uma variável que pode mudar de valor depois. O const é usado para criar um valor que não pode ser alterado. O sinal = serve para colocar um valor dentro da variável. O console.log é usado para mostrar o resultado na tela. O erro acontece quando tentamos mudar o valor de uma const, fazendo o código dar erro.

